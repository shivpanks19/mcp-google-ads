# Keyword plan research — automation run log

**Date:** 2026-05-27  
**Agent spec:** `Process-agents/references/keyword-plan-research.md`  
**Status:** **Blocked — no Google Ads credentials in this environment**

## What ran

1. **`list_accounts`** (MCP `my-server`) — failed: OAuth / service-account configuration not available for unattended use. Error indicated: use `GOOGLE_ADS_AUTH_TYPE=service_account` and `GOOGLE_ADS_CREDENTIALS_JSON` for headless execution.
2. **Keyword Planner API** — not invoked; requires the same credential material as above plus a **Basic or Standard** developer token.

## Required configuration (next successful run)

- `GOOGLE_ADS_DEVELOPER_TOKEN` (approved tier)
- `GOOGLE_ADS_AUTH_TYPE=service_account`
- `GOOGLE_ADS_CREDENTIALS_JSON` — service account JSON on one line, with the service account invited in Google Ads (**Tools & settings → Access and security → Users**)
- Target **`customer_id`** (10 digits); optional `GOOGLE_ADS_LOGIN_CUSTOMER_ID` for MCC flows

## Suggested seeds (when unblocked)

Aligned with repo examples (`docs/keyword-plan-examples.md`): enterprise AV / collaboration / education hardware in India — e.g. `microsoft teams room setup`, `video conferencing solution enterprise`, `digital signage mumbai`, `yealink dealer maharashtra`. Adjust to the client’s actual offer and landing pages.

## Unit tests

`test_keyword_plan_tools.py`: **17 passed** (2026-05-27, after `python3.12-venv` + `.venv` + `pip install -r requirements.txt` + `pytest`). Run from repo root: `.venv/bin/python -m pytest test_keyword_plan_tools.py -q`.
